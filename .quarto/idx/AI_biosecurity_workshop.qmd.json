{"title":"LLM analysis of a biosecurity corpus","markdown":{"yaml":{"title":"LLM analysis of a biosecurity corpus","editor":"source","date":"today","bibliography":["AI-biosecurity.bib"],"csl":"apa-tr.csl","execute":{"echo":true,"warning":false,"message":false,"freeze":"auto"},"format":{"html":{"embed-resources":true,"toc":true,"toc-depth":3,"toc-location":"left","code-fold":true,"code-tools":true,"output-file":"index.html","fig-cap-location":"margin","tbl-cap-location":"margin","fig-width":5.5}},"author":[{"name":"Christopher E Buddenhagen","affiliation":"Bioeconomy Science Institute, Ruakura, Hamilton, New Zealand"}]},"headingText":"Introduction","containsRefs":false,"markdown":"\n\n```{r setup}\n\nrequired_packages <- c(\n  \"tidyverse\",\"readxl\",\"janitor\",\"httr\",\"jsonlite\",\n  \"progressr\",\"tictoc\",\"openai\",\"gt\",\"knitr\"\n)\n\ninstalled <- rownames(installed.packages())\n\nfor (pkg in required_packages) {\n  if (!pkg %in% installed) install.packages(pkg)\n}\n\ninvisible(lapply(required_packages, library, character.only = TRUE))\n```\n\n\n\n::: {.callout-important}\n### Download Workshop Materials\n\nAll files needed for the Biosecurity Symposium workshop (University of Hawaii, 14-19 June 2026) are available here:\n\n👉 https://github.com/cbud/biosecurity-symposium/archive/refs/heads/main.zip\n\nDownload and unzip the file, then open the project in RStudio.\n:::\n\n### Required files\n\nThis workshop uses:\n\n- `corpus/` (text data)\n- `functions/` (helper functions)\n- `prompts/` (prompt templates)\n- `AI biosecurity workshop generalized.R` (main script)\n\nAll are included in the download. You can generate your own API key (not free) at: \\\n<https://openai.com/api>. One can be provided during the workshop.\n\n\n## LLM‑based extraction of biosecurity information\n\nThis document presents a generalised LLM pipeline for extracting structured information from bibliographic datasets. It supports scalable literature review and risk assessment workflows and was applied in rapid hazard ranking of invasive plant species [@buddenhagenValidatingRapidAlgorithmic2025].\n\nThe model uses three variables:\n\n- scientific publication counts  \n- climate suitability  \n- species distribution  \n\nto rank potential invasive taxa.\n\n@buddenhagenValidatingRapidAlgorithmic2025 introduced a rapid, algorithmic weed hazard ranking model (CPG) that uses widely available data. It leverages two well-supported predictors of weediness: weediness elsewhere and climate suitability [@caleyApplicationEvaluationClassification2006; @hulmeWeedRiskAssessment2012; @weberAnalysisAssessmentOutcomes2009; @randallCanPlantsCultural2016].\n\nEvidence shows that an organism’s invasion history (i.e., weediness elsewhere) generally outperforms biological traits (or attributes) as a predictor of risk. This is partly because selecting consistent trait-based variables is difficult across organism groups, and such data are often incomplete or unavailable.\n\nThe model estimates hazard by combining:\n\n1. climatic suitability (C) in the receiving region  \n2. the number of weed-related scientific publications (P), derived using LLMs as a proxy for impact and concern  \n3. global occurrence counts (G), reflecting ecological generalism and spread potential  \n\nHigh-scoring species can then be prioritised for more detailed risk assessments to evaluate potential harms, affected stakeholders, management feasibility, costs, and benefits.\n\n### Steps\n\n- Compile a dataframe with the corpus (text for querying with AI), with one row per source  \n- Develop a prompt for use with an OpenAI model (e.g. GPT‑4o‑mini)  \n- Run the prompts across the dataframe  \n- Standardise names to the GBIF backbone  \n\n## Background on Large Language Models (LLMs)\n\nMany modern large language models (LLMs) can be accessed via APIs that use a *pay-as-you-go* pricing model. Costs are typically based on the number of **tokens** processed.\n\nA *token* is a unit of text used internally by LLMs. It is not the same as a word; instead, it often corresponds to word pieces (for example, parts of words or short sequences of characters). This tokenization allows models to efficiently process and generate text.\n\nWhen using an API, you are generally charged for: - **Input tokens** (text sent to the model), and\\\n- **Output tokens** (text generated by the model)\n\nPricing varies by model and provider. For example, OpenAI publishes current pricing here:\\\n<https://developers.openai.com/api/docs/pricing>\n\nIn this project, we use the `gpt-4o-mini` model due to its balance of cost, speed, and capability. It provides relatively concise outputs and seems to respond well to the constraints in the prompts. Other models ( e.g., newer releases like gpt-5 or higher) would require changes to the code and prompts, as APIs and model interfaces can evolve over time. I tried, it didn't work.\n\nTo access an LLM API, you also need an **API key**. This is a private credential that authenticates your requests and links usage to your account. It should be stored securely (e.g., in environment variables) and never committed to version control.\n\nSimilar API-based access patterns are available across multiple providers, including OpenAI, Anthropic, Google, and others.\n\n# Running an example\n\n## Example data set\n\nThis pipeline used bibliometric data downloaded from the Scopus GUI, the query structure was: TITLE-ABS-KEY ( weed\\* OR invas\\* OR invad\\* OR \"alien plant*\" OR \"non-native plant*\" OR \"exotic plant*\" OR \"introduced plant*\" OR naturalised OR naturalized ) AND TITLE-ABS-KEY ( plant\\* OR vegetation OR species ) AND NOT TITLE-ABS-KEY ( insect\\* OR animal\\* OR bacter\\* OR virus\\* ). The full scopus had 156,000 abstracts - these had to be downloaded 20,000 at a time. Processing this amount of data cost about \\$40 USD and took three days. For the workshop I select a subset of 100 random articles with invasive plants or weeds mentioned and 100 that were determined by AI to be off-topic.\n\nThe species names (Latin binomials) in the abstract have been manually reviewed and are entered one species per row, with repeated bibliometric data if more than one species is mentioned in the abstract. To use the typical R language the data is long with respect to unique manually identified species.\n\n```{r, warning=FALSE, echo=FALSE}\n\ncorpus<-read_xlsx(\"corpus/scopus_sample_data_set.xlsx\") %>% distinct() %>% clean_names()\n\n#You must have title_abstract field in your dataframe for this pipeline - in this case you can include keyword fields.\n\n#dput(names(corpus))\n\n\n\n\n```\n\nThe corpus contains `r length(unique(corpus$eid))` records and\n`r length(unique(corpus$manual_calls_weeds))` manually identified species.\n\n\n## Prompt set up\n\nThis AI pipeline uses a chat completion approach with an LLM to populate columns in a dataframe through structured prompts. Each prompt consists of two components: **system text** (instructions that define behaviour and constraints) and **user text** (the specific task applied to each record).\n\nPrompts can be written manually or generated iteratively by asking an LLM to refine or expand an example prompt to cover multiple topics or variables. In this workflow, the initial prompt is designed to classify whether an article is relevant (“on topic”). Subsequent extraction prompts are only applied to records that pass this filter, reducing both runtime and token usage. This filtering step is optional but recommended for efficiency.\n\nThe prompt configuration is stored as a `topic_settings` object in the code, with the corresponding prompt defined in:\n \"prompts/prompt_1.R\".\n \n When setting up prompts, you should explicitly define:\n- the task (e.g. classification, entity extraction, counting)\n- the expected output format (e.g. “yes/no”, delimited lists, or structured fields)\n- any constraints or definitions (e.g. what qualifies as “weed-related”)\n- how to handle uncertainty (e.g. return “no” or “unknown”)\n\nClear, consistent instructions improve reproducibility and downstream parsing.\n\n\n## Run the LLM‑based data recovery\n\nTo run the pipeline, configure the user settings that control data input/output, model behaviour, and processing workflow.\n\nKey elements to specify:\n\n- **API key**: stored in a local text file and referenced via `api_key_file`  \n- **Input data**: ensure the dataframe includes a column named `title_abstract` containing the text to analyse  \n- **Output directory**: where results will be written  \n- **Model selection**: e.g. `gpt-4o-mini`, balancing cost and performance  \n- **Chunk size**: splits processing into batches to improve stability and enable restart from partial runs  \n- **Topic filter settings**: column name and pattern defining “on topic” records  \n- **Entity extraction settings**: column name and delimiter for parsed outputs  (in this case we mean species - since it is tied to the taxonomy)\n- **Taxonomic standardisation**: e.g. GBIF kingdom for downstream name matching  \n\nChunking is especially important for large datasets. For example, with ~1000 articles per chunk, each topic may take approximately 9–15 minutes. If a run fails, chunking allows you to resume without reprocessing completed records (with some changes to the script).\n\n```{r AI extraction}\n## AI biosecurity workshop generalized\n\n# ===========================================================\n# LLM ANALYSIS PIPELINE — GENERALISED SCRIPT\n# ===========================================================\n\n# -------------------------------\n# USER SETTINGS (EDIT THIS BLOCK)\n# -------------------------------\n\nsettings <- list(\n  \n  # INPUT DATA\n  input_file = \"corpus/scopus_sample_data_set.xlsx\",\n  \n  # OUTPUT\n  out_dir = \"./outputs/example_ai_run\",\n  \n  # MODEL + PROCESSING\n  model = \"gpt-4o-mini\",\n  chunk_size = 100,\n  \n  # TOPIC FILTER COLUMN (from topic_settings$output_column)\n  on_topic_column = \"on_topic_parsed\",   # <-- changeable\n  \n  # STRING THAT DEFINES \"YES\"\n  on_topic_pattern = \"^yes\",\n  \n  # ENTITY COLUMN (parsed names from AI)\n  entity_column = \"weed_name_parsed\",            # <-- changeable\n  entity_delim = \";\",\n  \n  # GBIF SETTINGS\n  gbif_kingdom = \"Plantae\",                      # change if needed\n  \n  # API KEY FILE\n  api_key_file = \"Biosecurity workshop api/Biosecurity_symposium_api.txt\"\n)\n\n# -------------------------------\n# LOAD DATA\n# -------------------------------\n\ncorpus <- read_xlsx(settings$input_file) %>%\n  distinct() %>%\n  clean_names() # < may need to read in with another function if this is in a csv format Scopus uses for outputs\n\n# Build text field\ncorpus <- corpus %>%\n  rowwise() %>%\n  mutate(\n    title_abstract = stringr::str_squish(\n      paste(\n        na.omit(c(title, abstract, author_keywords, index_keywords)),\n        collapse = \" \"\n      )\n    )\n  ) %>%\n  ungroup()\n\ndata_pubs_all <- corpus\n\ndata_pubs <- data_pubs_all %>%\n  distinct(eid, .keep_all = TRUE) %>%\n  mutate(row_id = row_number())\n\n# ===========================================================\n# API KEY\n# ===========================================================\n\nif (file.exists(settings$api_key_file)) {\n  Sys.setenv(OPENAI_API_KEY = readLines(settings$api_key_file, warn = FALSE))\n} else {\n  stop(\"API key file not found.\")\n}\n\n# ===========================================================\n# LOAD PROMPTS + FUNCTIONS\n# ===========================================================\n\nsource(\"prompts/prompt_1.R\")\nsource(\"functions/f_ai_text_analysis.R\")\nsource(\"functions/f_get_gbif_backbone_ids_from_df.R\")\n\n# -------------------------------\n# BUILD title_abstract FIELD\n# -------------------------------\ncorpus <- corpus %>%\n  rowwise() %>%\n  mutate(\n    title_abstract = stringr::str_squish(\n      paste(na.omit(c(title, abstract, author_keywords, index_keywords)),\n            collapse = \" \")\n    )\n  ) %>%\n  ungroup()\n\ndata_pubs_all <- corpus\n\ndata_pubs <- data_pubs_all %>%\n  distinct(eid, .keep_all = TRUE) %>%\n  mutate(row_id = row_number())\n\n# -------------------------------\n# CREATE OUTPUT DIR\n# -------------------------------\nif (!dir.exists(settings$out_dir)) dir.create(settings$out_dir, recursive = TRUE)\n\n# ===========================================================\n# 1. RUN TOPIC FILTER (ROW 1 OF topic_settings)\n# ===========================================================\ntic()\n\ntopic_category <- topic_settings$topic_category[1]\noutput_column  <- topic_settings$output_column[1]\nsystem_text    <- topic_settings$system_text[1]\nuser_text      <- topic_settings$user_text[1]\n\ntopic_dir <- file.path(settings$out_dir, topic_category)\n\n# ✅ CLEAN DIRECTORY (prevents duplicate chunk stacking)\nif (dir.exists(topic_dir)) unlink(topic_dir, recursive = TRUE)\ndir.create(topic_dir, recursive = TRUE)\n\nchunks <- split(data_pubs,\n                ceiling(seq_len(nrow(data_pubs)) / settings$chunk_size))\n\nfor (i in seq_along(chunks)) {\n  \n  message(\"Processing \", topic_category, \" | Chunk \", i)\n  \n  result_vector <- f_ai_text_analysis(\n    chunks[[i]]$title_abstract,\n    settings$model,\n    system_text,\n    user_text\n  )\n  \n  chunks[[i]][[output_column]] <- result_vector\n  \n  saveRDS(\n    chunks[[i]],\n    file = file.path(topic_dir, paste0(topic_category, \"_chunk_\", i, \".rds\"))\n  )\n  \n  # ✅ MEMORY CLEANING\n  rm(result_vector)\n  gc()\n}\n\n# ✅ FREE CHUNKS FROM MEMORY\nrm(chunks)\ngc()\n\n# ✅ LOAD ONLY chunk files\nfiles <- list.files(topic_dir, full.names = TRUE, pattern = \"\\\\.rds$\")\n\non_topic_final <- purrr::map_dfr(files, readRDS) %>%\n  distinct(eid, .keep_all = TRUE)   # ✅ safeguard against duplication\n\n# ✅ CLEAN TEMP OBJECTS\nrm(files)\ngc()\n\nsaveRDS(\n  on_topic_final,\n  file = file.path(topic_dir, paste0(topic_category, \"_final_results.rds\"))\n)\n\ntoc()\n\n# -------------------------------\n# FILTER ON-TOPIC (GENERALIZED)\n# -------------------------------\non_topic_pubs <- on_topic_final %>%\n  filter(stringr::str_detect(\n    tolower(.data[[settings$on_topic_column]]),\n    settings$on_topic_pattern\n  ))\n\n# ✅ CLEAN UNUSED OBJECT\nrm(on_topic_final)\ngc()\n\n# ===========================================================\n# 2. RUN REMAINING AI TASKS\n# ===========================================================\ntic()\n\nfor (j in 2:nrow(topic_settings)) {\n  \n  topic_category <- topic_settings$topic_category[j]\n  output_column  <- topic_settings$output_column[j]\n  system_text    <- topic_settings$system_text[j]\n  user_text      <- topic_settings$user_text[j]\n  \n  topic_dir <- file.path(settings$out_dir, topic_category)\n  \n  # ✅ CLEAN DIRECTORY BEFORE RUN\n  if (dir.exists(topic_dir)) unlink(topic_dir, recursive = TRUE)\n  dir.create(topic_dir, recursive = TRUE)\n  \n  chunks <- split(on_topic_pubs,\n                  ceiling(seq_len(nrow(on_topic_pubs)) / settings$chunk_size))\n  \n  for (i in seq_along(chunks)) {\n    \n    message(\"Processing \", topic_category, \" | Chunk \", i)\n    \n    result_vector <- f_ai_text_analysis(\n      chunks[[i]]$title_abstract,\n      settings$model,\n      system_text,\n      user_text\n    )\n    \n    chunks[[i]][[output_column]] <- result_vector\n    \n    saveRDS(\n      chunks[[i]],\n      file = file.path(topic_dir, paste0(topic_category, \"_chunk_\", i, \".rds\"))\n    )\n    \n    # ✅ MEMORY CLEANING (critical)\n    rm(result_vector)\n    gc()\n  }\n  \n  # ✅ REMOVE CHUNKS FROM MEMORY\n  rm(chunks)\n  gc()\n  \n  files <- list.files(topic_dir, full.names = TRUE, pattern = \"\\\\.rds$\")\n  \n  final_df <- purrr::map_dfr(files, readRDS) %>%\n    distinct(eid, .keep_all = TRUE)  # ✅ safeguard\n  \n  # ✅ CLEAN FILE LIST\n  rm(files)\n  gc()\n  \n  saveRDS(\n    final_df,\n    file = file.path(topic_dir, paste0(topic_category, \"_final_results.rds\"))\n  )\n  \n  # ✅ FREE MEMORY BEFORE NEXT LOOP\n  rm(final_df)\n  gc()\n}\n\ntoc()\n\n```\n\nAfter you run the ai recovery the data is stored for each chunk in separate folders for each topic and needs to be combined into a master file that stores the topic outputs described in the prompt and the original data. This code should work no matter how many prompt topics you are running. The script here takes about 5 minutes. An example script is also here: \"AI biosecurity workshop generalized.R\".\n\n```{r combine ai chunks}\n# ===========================================================\n# MERGE AI RESULTS \n# ===========================================================\n\nmerge_ai_results <- function(topic_settings, out_dir, join_col = \"eid\") {\n  \n  results_list <- purrr::pmap(\n    topic_settings,\n    function(topic_category, output_column, system_text, user_text) {\n      \n      results_file <- file.path(\n        out_dir,\n        topic_category,\n        paste0(topic_category, \"_final_results.rds\")\n      )\n      \n      if (!file.exists(results_file)) return(NULL)\n      \n      readRDS(results_file) %>%\n        distinct(.data[[join_col]], .keep_all = TRUE) %>%\n        select(all_of(c(join_col, output_column)))\n    }\n  )\n  \n  purrr::compact(results_list)\n}\n\nai_results <- merge_ai_results(topic_settings, settings$out_dir)\n\ndata_pubs_final <- purrr::reduce(\n  ai_results,\n  dplyr::left_join,\n  by = \"eid\",\n  .init = data_pubs_all\n) %>%\n  distinct()\n\nsaveRDS(data_pubs_final,\n        file = file.path(settings$out_dir, \"data_pubs_final.rds\"))\n```\n\n## Recover GBIF standard taxonomy\n\nIn our pipelines we standardize all Latin binomials to the [GBIF backbone taxonomy](https://www.gbif.org/dataset/d7dddbf4-2cf0-4f39-9b2a-bb099caae36c#description). This involves making the data long (multiple taxa are separated by semicolons in each row) and then extracting the names from the GBIF API. If a species isn't resolved by the backbone extraction process it is stored in the output directory you specified: `r settings$out_dir`.\n\n```{r taxonomy standardisation}\n\n\n# ===========================================================\n# MERGE AI RESULTS AND GET GBIF NAMES\n# ===========================================================\n\nmerge_ai_results <- function(topic_settings, out_dir, join_col = \"eid\") {\n  \n  results_list <- purrr::pmap(\n    topic_settings,\n    function(topic_category, output_column, system_text, user_text) {\n      \n      results_file <- file.path(\n        out_dir,\n        topic_category,\n        paste0(topic_category, \"_final_results.rds\")\n      )\n      \n      if (!file.exists(results_file)) return(NULL)\n      \n      readRDS(results_file) %>%\n        distinct(.data[[join_col]], .keep_all = TRUE) %>%\n        select(all_of(c(join_col, output_column)))\n    }\n  )\n  \n  purrr::compact(results_list)\n}\n\nai_results <- merge_ai_results(topic_settings, settings$out_dir)\n\ndata_pubs_final <- purrr::reduce(\n  ai_results,\n  dplyr::left_join,\n  by = \"eid\",\n  .init = data_pubs_all\n) %>%\n  distinct()\n\nsaveRDS(data_pubs_final,\n        file = file.path(settings$out_dir, \"data_pubs_final.rds\"))\n\n# ===========================================================\n# LONG FORMAT + GBIF (FULLY GENERALISED)\n# ===========================================================\n\ndata_pubs_long <- data_pubs_final %>%\n  tidyr::separate_longer_delim(\n    .data[[settings$entity_column]],\n    delim = settings$entity_delim\n  ) %>%\n  mutate(\n    entity_clean = stringr::str_squish(.data[[settings$entity_column]])\n  ) %>%\n  filter(!is.na(entity_clean),\n         entity_clean != \"\",\n         entity_clean != \"NA\") %>%\n  distinct(entity_clean, eid, .keep_all = TRUE)\n\ngbif_results <- f_get_gbif_backbone_ids_from_df(\n  df = data_pubs_long,\n  species_col = \"entity_clean\",\n  kingdom = settings$gbif_kingdom,\n  use_progress = FALSE,\n  save = FALSE\n)\n\n# OPTIONAL FILTER\ngbif_results <- gbif_results %>%\n  filter(kingdom_gbif == settings$gbif_kingdom)\n\n# JOIN BACK\ngbif_results_ai <- gbif_results %>%\n  left_join(data_pubs_long,\n            by = c(\"user_supplied_name\" = \"entity_clean\"))\n\n# SAVE\nsaveRDS(\n  gbif_results_ai,\n  file = file.path(\n    settings$out_dir,\n    paste0(\"gbif_results_\", Sys.Date(), \".rds\")\n  )\n)\n```\n\nIn these abstracts there were `r nrow(data_pubs_long %>% filter(on_topic_parsed==\"yes\") %>% select(eid) %>% distinct())` articles regarded as relevant. There were `r length(unique(data_pubs_long$weed_name_parsed))` unique names in the parsed name list before using the backbone taxonomy and `r length(unique(gbif_results_ai$species_gbif))` accepted species names after standardisation to the backbone taxonomy and restricting to species level.\n\n## Table of articles per species\n\nIn our usual application of this we count the number of articles per species in our corpus [@tbl-unique-article-ids-per-species].\n\n```{r}\n#| label: tbl-unique-article-ids-per-species\n#| tbl-cap: \"Number of unique articles per species\"\n\n\np_table_ai <- gbif_results_ai %>%\n  filter(!is.na(species_gbif)) %>%\n  group_by(species_gbif) %>%\n  summarise(\n    publication_count = n_distinct(eid),\n    names_used = paste(sort(unique(user_supplied_name)), collapse = \"; \"),\n    .groups = \"drop\"\n  ) %>%\n  arrange(desc(publication_count))\n\np_table_ai %>%\n  gt() %>%\n  tab_header(title = \"Publication counts by accepted species (GBIF)\")\n\nsaveRDS(\n  p_table_ai,\n  file = file.path(settings$out_dir,\n                   paste0(\"article_counts_by_species_\", Sys.Date(), \".rds\"))\n)\n\n\n```\n\nIf you want to modify the prompt for a different set of questions you can potentially use an LLM provider to create new prompts in the correct format, paste the prompt that works and suggest new prompts. Or you can run another example I provided for prompt_2 (after changing the output directory).\n","srcMarkdownNoYaml":"\n\n```{r setup}\n\nrequired_packages <- c(\n  \"tidyverse\",\"readxl\",\"janitor\",\"httr\",\"jsonlite\",\n  \"progressr\",\"tictoc\",\"openai\",\"gt\",\"knitr\"\n)\n\ninstalled <- rownames(installed.packages())\n\nfor (pkg in required_packages) {\n  if (!pkg %in% installed) install.packages(pkg)\n}\n\ninvisible(lapply(required_packages, library, character.only = TRUE))\n```\n\n# Introduction\n\n\n::: {.callout-important}\n### Download Workshop Materials\n\nAll files needed for the Biosecurity Symposium workshop (University of Hawaii, 14-19 June 2026) are available here:\n\n👉 https://github.com/cbud/biosecurity-symposium/archive/refs/heads/main.zip\n\nDownload and unzip the file, then open the project in RStudio.\n:::\n\n### Required files\n\nThis workshop uses:\n\n- `corpus/` (text data)\n- `functions/` (helper functions)\n- `prompts/` (prompt templates)\n- `AI biosecurity workshop generalized.R` (main script)\n\nAll are included in the download. You can generate your own API key (not free) at: \\\n<https://openai.com/api>. One can be provided during the workshop.\n\n\n## LLM‑based extraction of biosecurity information\n\nThis document presents a generalised LLM pipeline for extracting structured information from bibliographic datasets. It supports scalable literature review and risk assessment workflows and was applied in rapid hazard ranking of invasive plant species [@buddenhagenValidatingRapidAlgorithmic2025].\n\nThe model uses three variables:\n\n- scientific publication counts  \n- climate suitability  \n- species distribution  \n\nto rank potential invasive taxa.\n\n@buddenhagenValidatingRapidAlgorithmic2025 introduced a rapid, algorithmic weed hazard ranking model (CPG) that uses widely available data. It leverages two well-supported predictors of weediness: weediness elsewhere and climate suitability [@caleyApplicationEvaluationClassification2006; @hulmeWeedRiskAssessment2012; @weberAnalysisAssessmentOutcomes2009; @randallCanPlantsCultural2016].\n\nEvidence shows that an organism’s invasion history (i.e., weediness elsewhere) generally outperforms biological traits (or attributes) as a predictor of risk. This is partly because selecting consistent trait-based variables is difficult across organism groups, and such data are often incomplete or unavailable.\n\nThe model estimates hazard by combining:\n\n1. climatic suitability (C) in the receiving region  \n2. the number of weed-related scientific publications (P), derived using LLMs as a proxy for impact and concern  \n3. global occurrence counts (G), reflecting ecological generalism and spread potential  \n\nHigh-scoring species can then be prioritised for more detailed risk assessments to evaluate potential harms, affected stakeholders, management feasibility, costs, and benefits.\n\n### Steps\n\n- Compile a dataframe with the corpus (text for querying with AI), with one row per source  \n- Develop a prompt for use with an OpenAI model (e.g. GPT‑4o‑mini)  \n- Run the prompts across the dataframe  \n- Standardise names to the GBIF backbone  \n\n## Background on Large Language Models (LLMs)\n\nMany modern large language models (LLMs) can be accessed via APIs that use a *pay-as-you-go* pricing model. Costs are typically based on the number of **tokens** processed.\n\nA *token* is a unit of text used internally by LLMs. It is not the same as a word; instead, it often corresponds to word pieces (for example, parts of words or short sequences of characters). This tokenization allows models to efficiently process and generate text.\n\nWhen using an API, you are generally charged for: - **Input tokens** (text sent to the model), and\\\n- **Output tokens** (text generated by the model)\n\nPricing varies by model and provider. For example, OpenAI publishes current pricing here:\\\n<https://developers.openai.com/api/docs/pricing>\n\nIn this project, we use the `gpt-4o-mini` model due to its balance of cost, speed, and capability. It provides relatively concise outputs and seems to respond well to the constraints in the prompts. Other models ( e.g., newer releases like gpt-5 or higher) would require changes to the code and prompts, as APIs and model interfaces can evolve over time. I tried, it didn't work.\n\nTo access an LLM API, you also need an **API key**. This is a private credential that authenticates your requests and links usage to your account. It should be stored securely (e.g., in environment variables) and never committed to version control.\n\nSimilar API-based access patterns are available across multiple providers, including OpenAI, Anthropic, Google, and others.\n\n# Running an example\n\n## Example data set\n\nThis pipeline used bibliometric data downloaded from the Scopus GUI, the query structure was: TITLE-ABS-KEY ( weed\\* OR invas\\* OR invad\\* OR \"alien plant*\" OR \"non-native plant*\" OR \"exotic plant*\" OR \"introduced plant*\" OR naturalised OR naturalized ) AND TITLE-ABS-KEY ( plant\\* OR vegetation OR species ) AND NOT TITLE-ABS-KEY ( insect\\* OR animal\\* OR bacter\\* OR virus\\* ). The full scopus had 156,000 abstracts - these had to be downloaded 20,000 at a time. Processing this amount of data cost about \\$40 USD and took three days. For the workshop I select a subset of 100 random articles with invasive plants or weeds mentioned and 100 that were determined by AI to be off-topic.\n\nThe species names (Latin binomials) in the abstract have been manually reviewed and are entered one species per row, with repeated bibliometric data if more than one species is mentioned in the abstract. To use the typical R language the data is long with respect to unique manually identified species.\n\n```{r, warning=FALSE, echo=FALSE}\n\ncorpus<-read_xlsx(\"corpus/scopus_sample_data_set.xlsx\") %>% distinct() %>% clean_names()\n\n#You must have title_abstract field in your dataframe for this pipeline - in this case you can include keyword fields.\n\n#dput(names(corpus))\n\n\n\n\n```\n\nThe corpus contains `r length(unique(corpus$eid))` records and\n`r length(unique(corpus$manual_calls_weeds))` manually identified species.\n\n\n## Prompt set up\n\nThis AI pipeline uses a chat completion approach with an LLM to populate columns in a dataframe through structured prompts. Each prompt consists of two components: **system text** (instructions that define behaviour and constraints) and **user text** (the specific task applied to each record).\n\nPrompts can be written manually or generated iteratively by asking an LLM to refine or expand an example prompt to cover multiple topics or variables. In this workflow, the initial prompt is designed to classify whether an article is relevant (“on topic”). Subsequent extraction prompts are only applied to records that pass this filter, reducing both runtime and token usage. This filtering step is optional but recommended for efficiency.\n\nThe prompt configuration is stored as a `topic_settings` object in the code, with the corresponding prompt defined in:\n \"prompts/prompt_1.R\".\n \n When setting up prompts, you should explicitly define:\n- the task (e.g. classification, entity extraction, counting)\n- the expected output format (e.g. “yes/no”, delimited lists, or structured fields)\n- any constraints or definitions (e.g. what qualifies as “weed-related”)\n- how to handle uncertainty (e.g. return “no” or “unknown”)\n\nClear, consistent instructions improve reproducibility and downstream parsing.\n\n\n## Run the LLM‑based data recovery\n\nTo run the pipeline, configure the user settings that control data input/output, model behaviour, and processing workflow.\n\nKey elements to specify:\n\n- **API key**: stored in a local text file and referenced via `api_key_file`  \n- **Input data**: ensure the dataframe includes a column named `title_abstract` containing the text to analyse  \n- **Output directory**: where results will be written  \n- **Model selection**: e.g. `gpt-4o-mini`, balancing cost and performance  \n- **Chunk size**: splits processing into batches to improve stability and enable restart from partial runs  \n- **Topic filter settings**: column name and pattern defining “on topic” records  \n- **Entity extraction settings**: column name and delimiter for parsed outputs  (in this case we mean species - since it is tied to the taxonomy)\n- **Taxonomic standardisation**: e.g. GBIF kingdom for downstream name matching  \n\nChunking is especially important for large datasets. For example, with ~1000 articles per chunk, each topic may take approximately 9–15 minutes. If a run fails, chunking allows you to resume without reprocessing completed records (with some changes to the script).\n\n```{r AI extraction}\n## AI biosecurity workshop generalized\n\n# ===========================================================\n# LLM ANALYSIS PIPELINE — GENERALISED SCRIPT\n# ===========================================================\n\n# -------------------------------\n# USER SETTINGS (EDIT THIS BLOCK)\n# -------------------------------\n\nsettings <- list(\n  \n  # INPUT DATA\n  input_file = \"corpus/scopus_sample_data_set.xlsx\",\n  \n  # OUTPUT\n  out_dir = \"./outputs/example_ai_run\",\n  \n  # MODEL + PROCESSING\n  model = \"gpt-4o-mini\",\n  chunk_size = 100,\n  \n  # TOPIC FILTER COLUMN (from topic_settings$output_column)\n  on_topic_column = \"on_topic_parsed\",   # <-- changeable\n  \n  # STRING THAT DEFINES \"YES\"\n  on_topic_pattern = \"^yes\",\n  \n  # ENTITY COLUMN (parsed names from AI)\n  entity_column = \"weed_name_parsed\",            # <-- changeable\n  entity_delim = \";\",\n  \n  # GBIF SETTINGS\n  gbif_kingdom = \"Plantae\",                      # change if needed\n  \n  # API KEY FILE\n  api_key_file = \"Biosecurity workshop api/Biosecurity_symposium_api.txt\"\n)\n\n# -------------------------------\n# LOAD DATA\n# -------------------------------\n\ncorpus <- read_xlsx(settings$input_file) %>%\n  distinct() %>%\n  clean_names() # < may need to read in with another function if this is in a csv format Scopus uses for outputs\n\n# Build text field\ncorpus <- corpus %>%\n  rowwise() %>%\n  mutate(\n    title_abstract = stringr::str_squish(\n      paste(\n        na.omit(c(title, abstract, author_keywords, index_keywords)),\n        collapse = \" \"\n      )\n    )\n  ) %>%\n  ungroup()\n\ndata_pubs_all <- corpus\n\ndata_pubs <- data_pubs_all %>%\n  distinct(eid, .keep_all = TRUE) %>%\n  mutate(row_id = row_number())\n\n# ===========================================================\n# API KEY\n# ===========================================================\n\nif (file.exists(settings$api_key_file)) {\n  Sys.setenv(OPENAI_API_KEY = readLines(settings$api_key_file, warn = FALSE))\n} else {\n  stop(\"API key file not found.\")\n}\n\n# ===========================================================\n# LOAD PROMPTS + FUNCTIONS\n# ===========================================================\n\nsource(\"prompts/prompt_1.R\")\nsource(\"functions/f_ai_text_analysis.R\")\nsource(\"functions/f_get_gbif_backbone_ids_from_df.R\")\n\n# -------------------------------\n# BUILD title_abstract FIELD\n# -------------------------------\ncorpus <- corpus %>%\n  rowwise() %>%\n  mutate(\n    title_abstract = stringr::str_squish(\n      paste(na.omit(c(title, abstract, author_keywords, index_keywords)),\n            collapse = \" \")\n    )\n  ) %>%\n  ungroup()\n\ndata_pubs_all <- corpus\n\ndata_pubs <- data_pubs_all %>%\n  distinct(eid, .keep_all = TRUE) %>%\n  mutate(row_id = row_number())\n\n# -------------------------------\n# CREATE OUTPUT DIR\n# -------------------------------\nif (!dir.exists(settings$out_dir)) dir.create(settings$out_dir, recursive = TRUE)\n\n# ===========================================================\n# 1. RUN TOPIC FILTER (ROW 1 OF topic_settings)\n# ===========================================================\ntic()\n\ntopic_category <- topic_settings$topic_category[1]\noutput_column  <- topic_settings$output_column[1]\nsystem_text    <- topic_settings$system_text[1]\nuser_text      <- topic_settings$user_text[1]\n\ntopic_dir <- file.path(settings$out_dir, topic_category)\n\n# ✅ CLEAN DIRECTORY (prevents duplicate chunk stacking)\nif (dir.exists(topic_dir)) unlink(topic_dir, recursive = TRUE)\ndir.create(topic_dir, recursive = TRUE)\n\nchunks <- split(data_pubs,\n                ceiling(seq_len(nrow(data_pubs)) / settings$chunk_size))\n\nfor (i in seq_along(chunks)) {\n  \n  message(\"Processing \", topic_category, \" | Chunk \", i)\n  \n  result_vector <- f_ai_text_analysis(\n    chunks[[i]]$title_abstract,\n    settings$model,\n    system_text,\n    user_text\n  )\n  \n  chunks[[i]][[output_column]] <- result_vector\n  \n  saveRDS(\n    chunks[[i]],\n    file = file.path(topic_dir, paste0(topic_category, \"_chunk_\", i, \".rds\"))\n  )\n  \n  # ✅ MEMORY CLEANING\n  rm(result_vector)\n  gc()\n}\n\n# ✅ FREE CHUNKS FROM MEMORY\nrm(chunks)\ngc()\n\n# ✅ LOAD ONLY chunk files\nfiles <- list.files(topic_dir, full.names = TRUE, pattern = \"\\\\.rds$\")\n\non_topic_final <- purrr::map_dfr(files, readRDS) %>%\n  distinct(eid, .keep_all = TRUE)   # ✅ safeguard against duplication\n\n# ✅ CLEAN TEMP OBJECTS\nrm(files)\ngc()\n\nsaveRDS(\n  on_topic_final,\n  file = file.path(topic_dir, paste0(topic_category, \"_final_results.rds\"))\n)\n\ntoc()\n\n# -------------------------------\n# FILTER ON-TOPIC (GENERALIZED)\n# -------------------------------\non_topic_pubs <- on_topic_final %>%\n  filter(stringr::str_detect(\n    tolower(.data[[settings$on_topic_column]]),\n    settings$on_topic_pattern\n  ))\n\n# ✅ CLEAN UNUSED OBJECT\nrm(on_topic_final)\ngc()\n\n# ===========================================================\n# 2. RUN REMAINING AI TASKS\n# ===========================================================\ntic()\n\nfor (j in 2:nrow(topic_settings)) {\n  \n  topic_category <- topic_settings$topic_category[j]\n  output_column  <- topic_settings$output_column[j]\n  system_text    <- topic_settings$system_text[j]\n  user_text      <- topic_settings$user_text[j]\n  \n  topic_dir <- file.path(settings$out_dir, topic_category)\n  \n  # ✅ CLEAN DIRECTORY BEFORE RUN\n  if (dir.exists(topic_dir)) unlink(topic_dir, recursive = TRUE)\n  dir.create(topic_dir, recursive = TRUE)\n  \n  chunks <- split(on_topic_pubs,\n                  ceiling(seq_len(nrow(on_topic_pubs)) / settings$chunk_size))\n  \n  for (i in seq_along(chunks)) {\n    \n    message(\"Processing \", topic_category, \" | Chunk \", i)\n    \n    result_vector <- f_ai_text_analysis(\n      chunks[[i]]$title_abstract,\n      settings$model,\n      system_text,\n      user_text\n    )\n    \n    chunks[[i]][[output_column]] <- result_vector\n    \n    saveRDS(\n      chunks[[i]],\n      file = file.path(topic_dir, paste0(topic_category, \"_chunk_\", i, \".rds\"))\n    )\n    \n    # ✅ MEMORY CLEANING (critical)\n    rm(result_vector)\n    gc()\n  }\n  \n  # ✅ REMOVE CHUNKS FROM MEMORY\n  rm(chunks)\n  gc()\n  \n  files <- list.files(topic_dir, full.names = TRUE, pattern = \"\\\\.rds$\")\n  \n  final_df <- purrr::map_dfr(files, readRDS) %>%\n    distinct(eid, .keep_all = TRUE)  # ✅ safeguard\n  \n  # ✅ CLEAN FILE LIST\n  rm(files)\n  gc()\n  \n  saveRDS(\n    final_df,\n    file = file.path(topic_dir, paste0(topic_category, \"_final_results.rds\"))\n  )\n  \n  # ✅ FREE MEMORY BEFORE NEXT LOOP\n  rm(final_df)\n  gc()\n}\n\ntoc()\n\n```\n\nAfter you run the ai recovery the data is stored for each chunk in separate folders for each topic and needs to be combined into a master file that stores the topic outputs described in the prompt and the original data. This code should work no matter how many prompt topics you are running. The script here takes about 5 minutes. An example script is also here: \"AI biosecurity workshop generalized.R\".\n\n```{r combine ai chunks}\n# ===========================================================\n# MERGE AI RESULTS \n# ===========================================================\n\nmerge_ai_results <- function(topic_settings, out_dir, join_col = \"eid\") {\n  \n  results_list <- purrr::pmap(\n    topic_settings,\n    function(topic_category, output_column, system_text, user_text) {\n      \n      results_file <- file.path(\n        out_dir,\n        topic_category,\n        paste0(topic_category, \"_final_results.rds\")\n      )\n      \n      if (!file.exists(results_file)) return(NULL)\n      \n      readRDS(results_file) %>%\n        distinct(.data[[join_col]], .keep_all = TRUE) %>%\n        select(all_of(c(join_col, output_column)))\n    }\n  )\n  \n  purrr::compact(results_list)\n}\n\nai_results <- merge_ai_results(topic_settings, settings$out_dir)\n\ndata_pubs_final <- purrr::reduce(\n  ai_results,\n  dplyr::left_join,\n  by = \"eid\",\n  .init = data_pubs_all\n) %>%\n  distinct()\n\nsaveRDS(data_pubs_final,\n        file = file.path(settings$out_dir, \"data_pubs_final.rds\"))\n```\n\n## Recover GBIF standard taxonomy\n\nIn our pipelines we standardize all Latin binomials to the [GBIF backbone taxonomy](https://www.gbif.org/dataset/d7dddbf4-2cf0-4f39-9b2a-bb099caae36c#description). This involves making the data long (multiple taxa are separated by semicolons in each row) and then extracting the names from the GBIF API. If a species isn't resolved by the backbone extraction process it is stored in the output directory you specified: `r settings$out_dir`.\n\n```{r taxonomy standardisation}\n\n\n# ===========================================================\n# MERGE AI RESULTS AND GET GBIF NAMES\n# ===========================================================\n\nmerge_ai_results <- function(topic_settings, out_dir, join_col = \"eid\") {\n  \n  results_list <- purrr::pmap(\n    topic_settings,\n    function(topic_category, output_column, system_text, user_text) {\n      \n      results_file <- file.path(\n        out_dir,\n        topic_category,\n        paste0(topic_category, \"_final_results.rds\")\n      )\n      \n      if (!file.exists(results_file)) return(NULL)\n      \n      readRDS(results_file) %>%\n        distinct(.data[[join_col]], .keep_all = TRUE) %>%\n        select(all_of(c(join_col, output_column)))\n    }\n  )\n  \n  purrr::compact(results_list)\n}\n\nai_results <- merge_ai_results(topic_settings, settings$out_dir)\n\ndata_pubs_final <- purrr::reduce(\n  ai_results,\n  dplyr::left_join,\n  by = \"eid\",\n  .init = data_pubs_all\n) %>%\n  distinct()\n\nsaveRDS(data_pubs_final,\n        file = file.path(settings$out_dir, \"data_pubs_final.rds\"))\n\n# ===========================================================\n# LONG FORMAT + GBIF (FULLY GENERALISED)\n# ===========================================================\n\ndata_pubs_long <- data_pubs_final %>%\n  tidyr::separate_longer_delim(\n    .data[[settings$entity_column]],\n    delim = settings$entity_delim\n  ) %>%\n  mutate(\n    entity_clean = stringr::str_squish(.data[[settings$entity_column]])\n  ) %>%\n  filter(!is.na(entity_clean),\n         entity_clean != \"\",\n         entity_clean != \"NA\") %>%\n  distinct(entity_clean, eid, .keep_all = TRUE)\n\ngbif_results <- f_get_gbif_backbone_ids_from_df(\n  df = data_pubs_long,\n  species_col = \"entity_clean\",\n  kingdom = settings$gbif_kingdom,\n  use_progress = FALSE,\n  save = FALSE\n)\n\n# OPTIONAL FILTER\ngbif_results <- gbif_results %>%\n  filter(kingdom_gbif == settings$gbif_kingdom)\n\n# JOIN BACK\ngbif_results_ai <- gbif_results %>%\n  left_join(data_pubs_long,\n            by = c(\"user_supplied_name\" = \"entity_clean\"))\n\n# SAVE\nsaveRDS(\n  gbif_results_ai,\n  file = file.path(\n    settings$out_dir,\n    paste0(\"gbif_results_\", Sys.Date(), \".rds\")\n  )\n)\n```\n\nIn these abstracts there were `r nrow(data_pubs_long %>% filter(on_topic_parsed==\"yes\") %>% select(eid) %>% distinct())` articles regarded as relevant. There were `r length(unique(data_pubs_long$weed_name_parsed))` unique names in the parsed name list before using the backbone taxonomy and `r length(unique(gbif_results_ai$species_gbif))` accepted species names after standardisation to the backbone taxonomy and restricting to species level.\n\n## Table of articles per species\n\nIn our usual application of this we count the number of articles per species in our corpus [@tbl-unique-article-ids-per-species].\n\n```{r}\n#| label: tbl-unique-article-ids-per-species\n#| tbl-cap: \"Number of unique articles per species\"\n\n\np_table_ai <- gbif_results_ai %>%\n  filter(!is.na(species_gbif)) %>%\n  group_by(species_gbif) %>%\n  summarise(\n    publication_count = n_distinct(eid),\n    names_used = paste(sort(unique(user_supplied_name)), collapse = \"; \"),\n    .groups = \"drop\"\n  ) %>%\n  arrange(desc(publication_count))\n\np_table_ai %>%\n  gt() %>%\n  tab_header(title = \"Publication counts by accepted species (GBIF)\")\n\nsaveRDS(\n  p_table_ai,\n  file = file.path(settings$out_dir,\n                   paste0(\"article_counts_by_species_\", Sys.Date(), \".rds\"))\n)\n\n\n```\n\nIf you want to modify the prompt for a different set of questions you can potentially use an LLM provider to create new prompts in the correct format, paste the prompt that works and suggest new prompts. Or you can run another example I provided for prompt_2 (after changing the output directory).\n"},"formats":{"html":{"identifier":{"display-name":"HTML","target-format":"html","base-format":"html"},"execute":{"fig-width":5.5,"fig-height":5,"fig-format":"retina","fig-dpi":96,"df-print":"default","error":false,"eval":true,"cache":null,"freeze":"auto","echo":true,"output":true,"warning":false,"include":true,"keep-md":false,"keep-ipynb":false,"ipynb":null,"enabled":null,"daemon":null,"daemon-restart":false,"debug":false,"ipynb-filters":[],"ipynb-shell-interactivity":null,"plotly-connected":true,"message":false,"engine":"knitr"},"render":{"keep-tex":false,"keep-typ":false,"keep-source":false,"keep-hidden":false,"prefer-html":false,"output-divs":true,"output-ext":"html","fig-align":"default","fig-pos":null,"fig-env":null,"code-fold":true,"code-overflow":"scroll","code-link":false,"code-line-numbers":false,"code-tools":true,"tbl-colwidths":"auto","merge-includes":true,"inline-includes":false,"preserve-yaml":false,"latex-auto-mk":true,"latex-auto-install":true,"latex-clean":true,"latex-min-runs":1,"latex-max-runs":10,"latex-makeindex":"makeindex","latex-makeindex-opts":[],"latex-tlmgr-opts":[],"latex-input-paths":[],"latex-output-dir":null,"link-external-icon":false,"link-external-newwindow":false,"self-contained-math":false,"format-resources":[],"notebook-links":true},"pandoc":{"standalone":true,"wrap":"none","default-image-extension":"png","to":"html","toc":true,"embed-resources":true,"toc-depth":3,"output-file":"index.html"},"language":{"toc-title-document":"Table of contents","toc-title-website":"On this page","related-formats-title":"Other Formats","related-notebooks-title":"Notebooks","source-notebooks-prefix":"Source","other-links-title":"Other Links","code-links-title":"Code Links","launch-dev-container-title":"Launch Dev Container","launch-binder-title":"Launch Binder","article-notebook-label":"Article Notebook","notebook-preview-download":"Download Notebook","notebook-preview-download-src":"Download Source","notebook-preview-back":"Back to Article","manuscript-meca-bundle":"MECA Bundle","section-title-abstract":"Abstract","section-title-appendices":"Appendices","section-title-footnotes":"Footnotes","section-title-references":"References","section-title-reuse":"Reuse","section-title-copyright":"Copyright","section-title-citation":"Citation","appendix-attribution-cite-as":"For attribution, please cite this work as:","appendix-attribution-bibtex":"BibTeX citation:","appendix-view-license":"View License","title-block-author-single":"Author","title-block-author-plural":"Authors","title-block-affiliation-single":"Affiliation","title-block-affiliation-plural":"Affiliations","title-block-published":"Published","title-block-modified":"Modified","title-block-keywords":"Keywords","callout-tip-title":"Tip","callout-note-title":"Note","callout-warning-title":"Warning","callout-important-title":"Important","callout-caution-title":"Caution","code-summary":"Code","code-tools-menu-caption":"Code","code-tools-show-all-code":"Show All Code","code-tools-hide-all-code":"Hide All Code","code-tools-view-source":"View Source","code-tools-source-code":"Source Code","tools-share":"Share","tools-download":"Download","code-line":"Line","code-lines":"Lines","copy-button-tooltip":"Copy to Clipboard","copy-button-tooltip-success":"Copied!","repo-action-links-edit":"Edit this page","repo-action-links-source":"View source","repo-action-links-issue":"Report an issue","back-to-top":"Back to top","search-no-results-text":"No results","search-matching-documents-text":"matching documents","search-copy-link-title":"Copy link to search","search-hide-matches-text":"Hide additional matches","search-more-match-text":"more match in this document","search-more-matches-text":"more matches in this document","search-clear-button-title":"Clear","search-text-placeholder":"","search-detached-cancel-button-title":"Cancel","search-submit-button-title":"Submit","search-label":"Search","toggle-section":"Toggle section","toggle-sidebar":"Toggle sidebar navigation","toggle-dark-mode":"Toggle dark mode","toggle-reader-mode":"Toggle reader mode","toggle-navigation":"Toggle navigation","crossref-fig-title":"Figure","crossref-tbl-title":"Table","crossref-lst-title":"Listing","crossref-thm-title":"Theorem","crossref-lem-title":"Lemma","crossref-cor-title":"Corollary","crossref-prp-title":"Proposition","crossref-cnj-title":"Conjecture","crossref-def-title":"Definition","crossref-exm-title":"Example","crossref-exr-title":"Exercise","crossref-ch-prefix":"Chapter","crossref-apx-prefix":"Appendix","crossref-sec-prefix":"Section","crossref-eq-prefix":"Equation","crossref-lof-title":"List of Figures","crossref-lot-title":"List of Tables","crossref-lol-title":"List of Listings","environment-proof-title":"Proof","environment-remark-title":"Remark","environment-solution-title":"Solution","listing-page-order-by":"Order By","listing-page-order-by-default":"Default","listing-page-order-by-date-asc":"Oldest","listing-page-order-by-date-desc":"Newest","listing-page-order-by-number-desc":"High to Low","listing-page-order-by-number-asc":"Low to High","listing-page-field-date":"Date","listing-page-field-title":"Title","listing-page-field-description":"Description","listing-page-field-author":"Author","listing-page-field-filename":"File Name","listing-page-field-filemodified":"Modified","listing-page-field-subtitle":"Subtitle","listing-page-field-readingtime":"Reading Time","listing-page-field-wordcount":"Word Count","listing-page-field-categories":"Categories","listing-page-minutes-compact":"{0} min","listing-page-category-all":"All","listing-page-no-matches":"No matching items","listing-page-words":"{0} words","listing-page-filter":"Filter","draft":"Draft"},"metadata":{"lang":"en","fig-responsive":true,"quarto-version":"1.8.25","exclude":["Biosecurity workshop api/**"],"theme":"cosmo","title":"LLM analysis of a biosecurity corpus","editor":"source","date":"today","bibliography":["AI-biosecurity.bib"],"csl":"apa-tr.csl","author":[{"name":"Christopher E Buddenhagen","affiliation":"Bioeconomy Science Institute, Ruakura, Hamilton, New Zealand"}],"toc-location":"left","fig-cap-location":"margin","tbl-cap-location":"margin"},"extensions":{"book":{"multiFile":true}}}},"projectFormats":["html"]}